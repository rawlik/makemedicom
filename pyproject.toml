[project]
name = "makemedicom"
readme = "README.md"
version = "0.0.8"
dependencies = ["numpy==1.*", "pydicom==2.4.2", "h5py==3.9.0", "tifffile"]

[tool.setuptools]
py-modules = ["makemedicom"]

[project.scripts]
makemedicom = "makemedicom:entrypoint"
