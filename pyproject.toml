[project]
name = "makemedicom"
readme = "README.md"
version = "0.0.5"
dependencies = ["numpy", "pydicom==2.4.2", "h5py==3.9.0"]

[tool.setuptools]
py-modules = ["makemedicom"]

[project.scripts]
makemedicom = "makemedicom:entrypoint"
